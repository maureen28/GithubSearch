import { GitTextDecoDirective } from './git-text-deco.directive';

describe('GitTextDecoDirective', () => {
  it('should create an instance', () => {
    const directive = new GitTextDecoDirective();
    expect(directive).toBeTruthy();
  });
});
